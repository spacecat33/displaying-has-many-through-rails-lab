<h2> Patient: <%= @patient.name %> </h2>
<h3>has the following appointments:</h3>

<ul>
    <% @patient.appointments.each do |appointment| %>
        <li><%=  appointment.readable_time %> -  with Dr <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> </li>
    <% end %>
</ul>